[workspace]
members = [
    "dsp",
    "embedded",
    "test_interface",
    "vst",
    "vst/xtask",
]
resolver = "2"

[profile.profiling]
debug = 2
inherits = "release"
strip = "none"

[profile.release]
lto = "thin"
strip = "symbols"
