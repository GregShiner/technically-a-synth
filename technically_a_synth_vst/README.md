# Technically A Synth Vst

## Building

After installing [Rust](https://rustup.rs/), you can compile Technically A Synth Vst as follows:

```shell
cargo xtask bundle technically_a_synth_vst --release
```
